#include <iostream>
#include <string>
#include <algorithm>
#include <math.h>
#include <vector>

using namespace std;

#define INF 1 << 10

int main() {

    long long N;
    cin >> N;
    
    long long A, B, people[N+1];

    for (long long i = 0; i < N; i++) {
        cin >> A >> B;
        for (long long j = A; j < B; j++) {
            days[j]++;
        }
    }



    
    return 0;
}